name = input("what is ur name :")#we asked the user for an input and then stored it in a variable
age = int(input("what is ur age :"))
age = age + 1# we didnt ran in error as we converted the input value of the str into int
age = age - 1
print("hello "+name)
print("u are "+str(age)+" years old")
